import React, { useState } from 'react'; import { Alert, Pressable, StyleSheet, Text, TextInput, View } from 'react-native'; import { Link, router } from 'expo-router'; import { useApp, type AppRole } from '@/context/AppContext'; import { useColors } from '@/hooks/useColors';
export default function Signup() { const { signup } = useApp(); const colors = useColors(); const [displayName, setDisplayName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [role, setRole] = useState<AppRole>('customer'); const [busy, setBusy] = useState(false); const submit = async () => { setBusy(true); try { await signup({ displayName, email, password, role }); router.replace(role === 'provider' ? '/provider-onboarding' : '/'); } catch (error) { Alert.alert('Unable to create account', error instanceof Error ? error.message : 'Please try again.'); } finally { setBusy(false); } }; return <View style={[styles.page, { backgroundColor: colors.background }]}><Text style={[styles.brand, { color: colors.primary }]}>SMART SERVE</Text><Text style={[styles.title, { color: colors.foreground }]}>Create account</Text><TextInput value={displayName} onChangeText={setDisplayName} placeholder="Your name" placeholderTextColor={colors.mutedForeground} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /><TextInput autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} placeholder="Email" placeholderTextColor={colors.mutedForeground} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /><TextInput secureTextEntry value={password} onChangeText={setPassword} placeholder="Password (12+ characters)" placeholderTextColor={colors.mutedForeground} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /><View style={styles.roles}>{(['customer', 'provider'] as AppRole[]).map((item) => <Pressable key={item} onPress={() => setRole(item)} style={[styles.role, { borderColor: role === item ? colors.primary : colors.border, backgroundColor: role === item ? colors.secondary : colors.card }]}><Text style={{ color: colors.foreground, fontFamily: 'Inter_600SemiBold' }}>{item === 'customer' ? 'Customer' : 'Service provider'}</Text></Pressable>)}</View><Pressable disabled={busy} onPress={submit} style={[styles.button, { backgroundColor: colors.primary, opacity: busy ? .7 : 1 }]}><Text style={{ color: colors.primaryForeground, fontFamily: 'Inter_700Bold' }}>{busy ? 'Creating account…' : 'Create account'}</Text></Pressable><Link href={'/login' as any} style={[styles.link, { color: colors.primary }]}>Already have an account? Sign in</Link></View>; }
const styles = StyleSheet.create({ page: { flex: 1, justifyContent: 'center', padding: 24, gap: 12 }, brand: { fontFamily: 'Inter_700Bold', fontSize: 12, letterSpacing: 2 }, title: { fontFamily: 'Inter_700Bold', fontSize: 32, marginBottom: 10 }, input: { borderWidth: 1, borderRadius: 15, minHeight: 52, paddingHorizontal: 15, fontFamily: 'Inter_400Regular' }, roles: { flexDirection: 'row', gap: 10 }, role: { flex: 1, minHeight: 54, borderRadius: 15, borderWidth: 1, justifyContent: 'center', alignItems: 'center' }, button: { alignItems: 'center', borderRadius: 15, minHeight: 52, justifyContent: 'center', marginTop: 4 }, link: { textAlign: 'center', marginTop: 8, fontFamily: 'Inter_600SemiBold' } });
