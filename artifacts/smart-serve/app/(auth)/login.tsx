import React, { useState } from 'react'; import { Alert, Pressable, StyleSheet, Text, TextInput, View } from 'react-native'; import { Link, router } from 'expo-router'; import { useApp } from '@/context/AppContext'; import { useColors } from '@/hooks/useColors';
export default function Login() { const { login } = useApp(); const colors = useColors(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [busy, setBusy] = useState(false); const submit = async () => { setBusy(true); try { await login(email, password); router.replace('/'); } catch (error) { Alert.alert('Unable to sign in', error instanceof Error ? error.message : 'Please try again.'); } finally { setBusy(false); } }; return <View style={[styles.page, { backgroundColor: colors.background }]}><Text style={[styles.brand, { color: colors.primary }]}>SMART SERVE</Text><Text style={[styles.title, { color: colors.foreground }]}>Welcome back</Text><Text style={[styles.copy, { color: colors.mutedForeground }]}>Sign in to manage your local services securely.</Text><TextInput autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail} placeholder="Email" placeholderTextColor={colors.mutedForeground} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /><TextInput secureTextEntry value={password} onChangeText={setPassword} placeholder="Password" placeholderTextColor={colors.mutedForeground} style={[styles.input, { color: colors.foreground, borderColor: colors.border }]} /><Pressable disabled={busy} onPress={submit} style={[styles.button, { backgroundColor: colors.primary, opacity: busy ? .7 : 1 }]}><Text style={{ color: colors.primaryForeground, fontFamily: 'Inter_700Bold' }}>{busy ? 'Signing in…' : 'Sign in'}</Text></Pressable><Link href={'/signup' as any} style={[styles.link, { color: colors.primary }]}>New to Smart Serve? Create an account</Link></View>; }
const styles = StyleSheet.create({ page: { flex: 1, justifyContent: 'center', padding: 24, gap: 14 }, brand: { fontFamily: 'Inter_700Bold', fontSize: 12, letterSpacing: 2 }, title: { fontFamily: 'Inter_700Bold', fontSize: 32 }, copy: { fontFamily: 'Inter_400Regular', fontSize: 14, marginBottom: 14 }, input: { borderWidth: 1, borderRadius: 15, minHeight: 52, paddingHorizontal: 15, fontFamily: 'Inter_400Regular' }, button: { alignItems: 'center', borderRadius: 15, minHeight: 52, justifyContent: 'center', marginTop: 4 }, link: { textAlign: 'center', marginTop: 8, fontFamily: 'Inter_600SemiBold' } });
